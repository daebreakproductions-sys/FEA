{
  "version": 2,
  "framework": "angular",
  "buildCommand": "npx ng build --configuration production",
  "outputDirectory": "www/browser",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
