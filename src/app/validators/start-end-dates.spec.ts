import { StartEndDatesValidator } from './start-end-dates';

describe('StartEndDates', () => {
  it('should create an instance', () => {
    expect(new StartEndDatesValidator()).toBeTruthy();
  });
});
